import {StyleSheet, Text, View, Alert, Button, Dimensions} from 'react-native';
import React from 'react';

const TestSomeProps = () => {

console.log(Dimensions.get('screen'))

  return (
    <View>
      
    </View>
  );
};

export default TestSomeProps;

const styles = StyleSheet.create({});
